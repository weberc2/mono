apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: serverstatus
  name: serverstatus
spec:
  ingressClassName: nginx
  rules:
    - host: serverstatus
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: serverstatus
                port:
                  number: 80
